{{>header}}
        <h2>{{title}}</h2>

        <form>
            <input placeholder="place name">
            <button>search</button>
        </form>
        <p>created by {{name}}</p>

        <p id="message-1"></p>
        <p id="message-2"></p>

{{>footer}}